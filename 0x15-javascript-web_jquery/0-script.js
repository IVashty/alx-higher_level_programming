#!/usr/bin/node
// Get the header element using document.querySelector
const header = document.querySelector('header');

// Set the color property of the style object to red
header.style.color = '#FF0000';
