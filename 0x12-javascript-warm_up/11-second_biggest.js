#!/usr/bin/node

//second largest number - algebra_precalculus
let array = process.argv.length([]);
for (let arrayOne = 2; arrayOne < length; arrayOne++) {
  return (Number(process.argv[arrayOne]));
}
if (array.length <= 1) {
  console.log(0);
} else {
  console.log(array.sort((arrayOne, arrayTwo) => { return arrayTwo - arrayOne}));
}
